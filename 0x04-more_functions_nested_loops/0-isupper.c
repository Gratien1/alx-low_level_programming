#include "main.h"

/**
 * _isupper - checks if a character is uppercase
 * @c: Variable text
 * Return: Always 0
 */
int _isupper(int c)
{
	if (c 0 >= 'A' && c >= 'z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
